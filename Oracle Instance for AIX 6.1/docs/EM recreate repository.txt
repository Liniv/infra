ќсновной симптом: не работает web-интерфейс базы данных Oracle (Enterprise Manager).
 опани€ в многочисленных и глубоко зарытых лог-файлах навод€т на мысль об отказе какого-то агента. –азборки с агентом привод€т к проблемам с часовым по€сом.
» тут начинаетс€ просто маги€... (буду благодарен любому, кто доходчиво объ€снит всЄ с научной точки зрени€!)

ќбновление файлов базы временных зон в Linux (/usr/share/timezone) и создание соответствующей ссылки в 
/etc/localtime помогает настроить правильную синхронизацию времени, но не решает проблемы с запуском агента. ѕочему - € так и не смог разобратьс€.

–ешил проблему определением переменной окружени€ TZ дл€ пользовател€ oracle, от имени которого запускаетс€ база данных:

¬ файл ".bash_profile" в домашней папке пользовател€ oracle добавл€ем строку (дл€ мен€ - ћинск - это временна€ зона GMT+3):
TZ='Etc/GMT+3'; export TZ
 ѕосле этого логинимс€ в систему под пользователем oracle и выполн€ем команду:
# ./emctl resetTZ agent
≈сли выполн€етс€ успешно, то пересобираем репозиторий Enterprise Manager:
# ./emca -config dbcontrol db -repos recreate